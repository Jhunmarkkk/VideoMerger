services:
  - type: web
    name: video-merger
    runtime: python
    buildCommand: apt-get install -y ffmpeg && pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: FFMPEG_PATH
        value: ffmpeg
